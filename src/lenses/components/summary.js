import React from 'react'

const SummaryComponent = ({label_}) => {
    return (
        <div className="summaryDiv">
            <p>{label_}</p>
        </div>
    )
}

export default SummaryComponent